; just simple example of main entry point

/* 
    This is multiline comment.
    It can span multiple lines.
    And it can be folded in editors that support code folding.
    As well as ZAP blocks ending with 'end'
    or some  ZAP directives.
*/

proc main()
end


