source "https://rubygems.org"

# GitHub Pages gem — pins Jekyll + plugins to versions GitHub Pages supports.
# This lets you preview the site locally with the same software GitHub uses.
#
# NOTE: we intentionally do NOT list `gem "just-the-docs"` here. The theme is
# loaded via `remote_theme: just-the-docs/just-the-docs` in _config.yml, which
# the jekyll-remote-theme plugin (bundled in github-pages) fetches at build
# time. Adding it as a separate gem breaks github-pages' locked dependency tree.
gem "github-pages", group: :jekyll_plugins

# Windows and JRuby do not include zoneinfo files, so bundle the tzinfo-data gem
# and associated library.
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end

# Performance-booster for watching directories on Windows
gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]
